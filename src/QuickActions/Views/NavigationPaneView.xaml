<!--Created by Matt Weber <matt@badecho.com>
    Copyright @ 2025 Bad Echo LLC. All rights reserved.

    Bad Echo Technologies are licensed under the
    GNU Affero General Public License v3.0.

    See accompanying file LICENSE.md or a copy at:
    https://www.gnu.org/licenses/agpl-3.0.html-->
<badEcho:View x:Class="BadEcho.QuickActions.Views.NavigationPaneView"
              x:ClassModifier="internal"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:badEcho="http://schemas.badecho.com/presentation/2022/02/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:vm="clr-namespace:BadEcho.QuickActions.ViewModels"
              mc:Ignorable="d"
              d:DesignStyle="{StaticResource DesignTimeControlStyle}"
              >
    <d:DesignProperties.DataContext>
        <vm:NavigationPaneViewModel>
            <vm:NavigationPaneViewModel.Items>
                <vm:NavigationTargetViewModel Name="home"
                                              IconGlyph="&#xE8F2;"
                                              />
                <vm:NavigationTargetViewModel Name="actions"
                                              IconGlyph="&#xE700;"
                                              />
            </vm:NavigationPaneViewModel.Items>
        </vm:NavigationPaneViewModel>
    </d:DesignProperties.DataContext>
    <ListBox x:Name="NavigationPane"
             Background="{StaticResource SolidBackgroundFillColorBaseBrush}"
             ItemsSource="{Binding Items}"
             SelectedItem="{Binding SelectedViewModel}"
             >
        <badEcho:FrameworkBehaviors.EventTriggers>
            <badEcho:EventTrigger EventName="SelectionChanged">
                <badEcho:CommandAction Command="{Binding SelectionCommand}"
                                       Parameter="{Binding ElementName=NavigationPane, Path=SelectedItem}"
                                       />
            </badEcho:EventTrigger>
        </badEcho:FrameworkBehaviors.EventTriggers>
    </ListBox>
</badEcho:View>

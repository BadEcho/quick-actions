<!--Created by Matt Weber <matt@badecho.com>
    Copyright @ 2025 Bad Echo LLC. All rights reserved.

    Bad Echo Technologies are licensed under the
    GNU Affero General Public License v3.0.

    See accompanying file LICENSE.md or a copy at:
    https://www.gnu.org/licenses/agpl-3.0.html-->
<badEcho:View x:Class="BadEcho.QuickActions.Views.MappingView"
              x:ClassModifier="internal"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:badEcho="http://schemas.badecho.com/presentation/2022/02/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:local="clr-namespace:BadEcho.QuickActions"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:sys="clr-namespace:System;assembly=mscorlib"
              xmlns:vm="clr-namespace:BadEcho.QuickActions.ViewModels"
              mc:Ignorable="d"
              d:DesignStyle="{StaticResource DesignTimeControlStyle}"
              >
    <d:DesignProperties.DataContext>
        <vm:MappingViewModel KeysText="Control + S">
            <vm:MappingViewModel.Actions>
                <local:ScriptAction Name="An Action" />
            </vm:MappingViewModel.Actions>
        </vm:MappingViewModel>
    </d:DesignProperties.DataContext>
    <badEcho:View.Resources>
        <badEcho:MultiBooleanConverter x:Key="BooleanOrConverter"
                                       Operation="Or"
                                       />
    </badEcho:View.Resources>
    <Grid>
        <Grid.Style>
            <Style TargetType="{x:Type Grid}">
                <Style.Triggers>
                    <MultiDataTrigger />
                    <DataTrigger Value="True">
                        <DataTrigger.Binding>
                            <MultiBinding Converter="{StaticResource BooleanOrConverter}">
                                <Binding ElementName="Keys"
                                         Path="(Validation.HasError)"
                                         />
                                <Binding ElementName="Actions"
                                         Path="(Validation.HasError)"
                                         />
                            </MultiBinding>
                        </DataTrigger.Binding>
                        <DataTrigger.Setters>
                            <Setter Property="Margin" Value="{StaticResource VerticalSpacing}" />
                        </DataTrigger.Setters>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.ColumnDefinitions>
            <ColumnDefinition SharedSizeGroup="KeysColumn" Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="Keys"
                 CaretIndex="{x:Static sys:Int32.MaxValue}"
                 Text="{Binding KeysText, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="KeysTextChanged"
                 Validation.ErrorTemplate="{StaticResource ErrorTextBlockTemplate}"
                 MinWidth="200"
                 Margin="0,0,16,0"
                 Grid.Column="0"
                 >
            <badEcho:FrameworkBehaviors.EventTriggers>
                <badEcho:EventTrigger EventName="PreviewKeyDown">
                    <badEcho:CommandAction Command="{Binding KeyInputCommand}" />
                </badEcho:EventTrigger>
            </badEcho:FrameworkBehaviors.EventTriggers>
            <badEcho:FrameworkBehaviors.Triggers>
                <badEcho:Trigger Property="Validation.HasError" Value="True">
                    <badEcho:OverrideResourceAction Name="TextControlBorderBrushFocused"
                                                    Value="{StaticResource ErrorTextControlBorderBrushFocused}"
                                                    />
                </badEcho:Trigger>
                <badEcho:Trigger Property="Validation.HasError" Value="False">
                    <badEcho:OverrideResourceAction Name="TextControlBorderBrushFocused" />
                </badEcho:Trigger>
            </badEcho:FrameworkBehaviors.Triggers>
        </TextBox>

        <ComboBox x:Name="Actions"
                  d:SelectedIndex="0"
                  ItemsSource="{Binding Actions}"
                  SelectedItem="{Binding SelectedAction, ValidatesOnNotifyDataErrors=True}"
                  Validation.ErrorTemplate="{StaticResource ErrorTextBlockTemplate}"
                  Margin="0,0,16,0"
                  Grid.Column="1"
                  />
        <Button Style="{StaticResource GarbageButtonStyle}"
                Command="{Binding DeleteCommand}"
                Grid.Column="2"
                />
    </Grid>
</badEcho:View>
